meta {
  name: Restock Inventory
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/inventory/restock
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "productId": "{{productId}}",
    "quantity": {{restockQty}}
  }
}
