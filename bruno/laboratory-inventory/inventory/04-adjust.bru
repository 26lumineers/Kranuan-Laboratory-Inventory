meta {
  name: Adjust Inventory
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/inventory/adjust
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "productId": "{{productId}}",
    "quantity": {{adjustQty}},
    "reason": "{{adjustReason}}"
  }
}
