meta {
  name: List Orders
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/orders
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

query {
  userId: {{userId}}
  roomId: {{roomId}}
  status: PENDING
}
