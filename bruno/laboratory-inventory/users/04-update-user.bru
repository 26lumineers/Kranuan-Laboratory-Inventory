meta {
  name: Update User
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/users/{{userId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "fullName": "Updated Name",
    "nickname": "New Nickname",
    "role": "ADMIN",
    "isActive": true
  }
}
