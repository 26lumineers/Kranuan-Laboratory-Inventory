services:
    backend:
        build:
            context: .
            dockerfile: Dockerfile.backend
        container_name: lab_backend
        restart: unless-stopped
        ports:
            - '3001:3001'
        environment:
            DATABASE_URL: postgresql://lab_user:strongpassword@postgres:5432/laboratory_inventory
        depends_on: []
